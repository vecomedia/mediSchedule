# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Set to "true" on non-Vercel hosts (Railway, Render, etc.)
AUTH_TRUST_HOST=

# Uncomment and fill in when adding Prisma + PostgreSQL
# DATABASE_URL=postgresql://user:password@host:5432/medischedule
